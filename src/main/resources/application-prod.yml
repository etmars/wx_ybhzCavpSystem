# 生产 profile：本机 HTTP 12380，对外 HTTPS 由 Nginx/反代终止
# 启动: java -jar xxx.jar --spring.profiles.active=prod
# 可选环境变量：SERVER_ADDRESS（默认 127.0.0.1，仅本机反代可访问）
server:
  port: 12380
  address: ${SERVER_ADDRESS:127.0.0.1}
